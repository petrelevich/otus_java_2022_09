management:
  endpoints:
    enabled-by-default: false
  endpoint:
    health:
      enabled: true
      probes:
        enabled: true

database:
  url: "jdbc:postgresql://localhost:5430/demoDB"
  user: "usr"
  pwd: "pwd"